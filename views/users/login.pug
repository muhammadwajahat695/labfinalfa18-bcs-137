extends ../layout

block content
  h1 ADMIN Login
  form(action="/login", method="post")
      .form-group     
          label(for="email") Email
          input#email(type="email", class="form-control", name="email", value="")
      .form-group     
          label(for="password") Password
          input#password(type="password", class="form-control", name="password", value="")
      input(type="submit", class="btn btn-info", value="Login") 